// Generated by CoffeeScript 1.10.0
(function() {
  var array1, array2, array3, array4, car, gender, hello, human, i, iLike, isPerson, item, j, k, l, len, len1, len2, len3, len4, longstring, m, name, person, ref, sex, type, types, types2,
    slice = [].slice;

  name = "Ravuthz";

  isPerson = true;

  person = function() {
    var agge;
    agge = 25;
    alert(name);
    return alert(age);
  };

  human = function(n) {
    return alert(n);
  };

  if (isPerson === true && name !== "Scott") {
    person();
  }

  if (isPerson !== false) {
    person();
  }

  if (isPerson !== false) {
    person();
  }

  if (name != null) {
    person();
  }

  if (typeof gender !== "undefined" && gender !== null) {
    gender = "male";
  }

  sex = "";

  if (sex == null) {
    sex = "male";
  }

  if (!isPerson) {
    person();
  }

  types = ['Rice', 'Crab', 'Cubumber'];

  type = ['Rice', 'Crab', 'Cubumber'];

  car = {
    name: "CRV",
    run: function() {
      return "fast";
    },
    year: 2001,
    engine: {
      core: "240",
      year: 2001,
      "class": "sport"
    }
  };

  alert("My name is " + name + " and my age is " + (20 + 5));

  longstring = "hello how are you? this the long paragraph.";

  hello = "<h1>\n    Hello World\n</h1>";


  /*
      Multiple line comments
   */

  for (i = 0, len = types.length; i < len; i++) {
    item = types[i];
    alert(item);
  }

  for (j = 0, len1 = types.length; j < len1; j++) {
    item = types[j];
    alert(item);
  }

  for (k = 0, len2 = types.length; k < len2; k++) {
    item = types[k];
    if (item !== 'Rice') {
      alert(item);
    }
  }

  for (l = 0, len3 = types.length; l < len3; l++) {
    item = types[l];
    if (item !== 'Crab') {
      item + "s";
    }
  }

  for (m = 0, len4 = types.length; m < len4; m++) {
    item = types[m];
    if (item !== 'Crab') {
      types = item + "s";
    }
  }

  types2 = (function() {
    var len5, o, results;
    results = [];
    for (o = 0, len5 = types.length; o < len5; o++) {
      item = types[o];
      if (item !== 'Rice') {
        results.push(item);
      }
    }
    return results;
  })();

  iLike = function() {
    var most, rest, second;
    most = arguments[0], second = arguments[1], rest = 3 <= arguments.length ? slice.call(arguments, 2) : [];
    alert("I like " + most + " the most");
    alert("I like " + second + " the second most");
    return alert("I also enjoy " + rest);
  };

  iLike.apply(null, types);

  array1 = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h'];

  array2 = array1.slice(0, 5);

  array3 = array1.slice(4);

  array4 = array1.slice(0);

  array2 = array1.slice(0, 4);

  array3 = array1.slice(4);

  [].splice.apply(array1, [2, 3].concat(ref = ['uc', 'ud', 'ue'])), ref;

  switch (colors) {
    case 'red':
      alert(Blue);
      break;
    case 'green':
      alert(name = 'Green');
      break;
    case 'blue':
    case 'black':
      alert('Blue or Black');
      break;
    default:
      alert('No colors');
  }

}).call(this);
